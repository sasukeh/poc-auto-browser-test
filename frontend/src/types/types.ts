export type MessageType = {
  message: string;
  isMan: boolean;
};

export type ItemType = {
  id: string;
  title: string;
  url: string;
  result: string;
  isError: boolean;
  dueDate: string;
};
